{
  "name": "buddy_workspace",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.15",
    "typescript": "^4.4.4"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "build:mcp": "pnpm -r build:mcp",
    "dev:app": "pnpm --filter buddy dev",
    "dev:ext": "nodemon pipeline.mjs",
    "mcp": "pnpm --filter mcp-core test",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
