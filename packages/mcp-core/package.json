{
  "name": "mcp-core",
  "version": "0.1.0",
  "description": "Core functionality for MCP (Message Control Protocol)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "dev": "tsc -w",
    "lint": "eslint src --ext .ts",
    "test": "node -r ts-node/register test.ts",
    "prepublishOnly": "pnpm run build"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "chalk": "^4.1.2",
    "dotenv": "^16.0.3",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "eslint": "^8.0.0",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
