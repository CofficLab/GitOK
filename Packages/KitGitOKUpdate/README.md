# KitGitOKUpdate

GitOK 应用更新工具包：`UpdateService` 封装 Sparkle 更新能力，`AppUpdateBootstrap` 负责启动与持有更新服务，`UpdateServiceStateMachine` 管理更新生命周期状态，`FeedURLDetector` / `UpdateFeedURLProvider` 提供 feed 地址探测。宿主级能力，不属于业务插件 Provider。
