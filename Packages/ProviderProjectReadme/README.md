# ProviderProjectReadme

Shared provider contract for requesting a project's rendered README view. The `PluginProjectReadme` package implements it, while consumers such as `PluginWorktreeClean` depend only on this package.

## Build

```bash
swift build
```
